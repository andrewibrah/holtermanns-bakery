# Holtermann's Bakery

Static marketing website for Holtermann's Bakery — Staten Island's family-owned bakery since 1878.

Built as plain HTML/CSS/JS. Deployed to GitHub Pages via GitHub Actions (see `.github/workflows/deploy.yml`).

## Local preview

```
python3 -m http.server 8080
```
Then open http://localhost:8080
